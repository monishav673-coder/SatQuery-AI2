# SATQUERY AI — Python dependencies
# Install: pip install -r requirements.txt

# ── Core web framework ────────────────────────────────────────────────────────
Flask==3.0.3
flask-cors==4.0.0
flask-jwt-extended==4.6.0
Flask-SQLAlchemy==3.1.1
Flask-Bcrypt==1.0.1

# ── Rate limiting ─────────────────────────────────────────────────────────────
flask-limiter==3.8.0

# ── Database ──────────────────────────────────────────────────────────────────
SQLAlchemy==2.0.30
alembic==1.13.2
# PostgreSQL driver — uncomment for production:
# psycopg2-binary==2.9.9

# ── Async job queue ───────────────────────────────────────────────────────────
rq==1.16.2
redis==5.0.7

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── HTTP / Networking ─────────────────────────────────────────────────────────
requests==2.32.3
urllib3==2.2.2

# ── Image processing ─────────────────────────────────────────────────────────
Pillow==10.3.0

# ── Geospatial (strongly recommended for GeoTIFF support) ─────────────────────
# Windows: pip install rasterio --find-links https://github.com/cgohlke/rasterio-build/releases
# Linux/Docker: pip install rasterio==1.3.10
# rasterio==1.3.10

# ── Scientific computing ──────────────────────────────────────────────────────
numpy==1.26.4
scipy==1.13.1

# ── PDF report generation ─────────────────────────────────────────────────────
reportlab==4.1.0

# ── WSGI server ───────────────────────────────────────────────────────────────
gunicorn==22.0.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-cov==5.0.0

# ── Deep learning (install separately in ml_service or when needed) ───────────
# torch==2.3.0
# torchvision==0.18.0
# transformers==4.41.2
# onnxruntime==1.18.0
